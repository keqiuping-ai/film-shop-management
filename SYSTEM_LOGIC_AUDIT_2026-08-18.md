# QUaD 全系统前后账与跨模块计算审计（2026-08-18）

## 审计边界

- 代码版本：`93b0419`
- 本地隔离数据库：已执行新增、出库、删除、收货、撤销等破坏性场景测试。
- Railway 正式数据：当前浏览器停在登录页，尚未读取正式数据库，因此本文不把代码风险写成正式数据已经错账。
- 本轮没有修改 Railway 数据、环境变量或部署。

## 业务数据主链路

1. Meta / Yelp / 短信客户进入 `customerConversations`。
2. 达到预约/到店条件后进入 `prospects`，生成施工单后进入 `jobs`。
3. 施工单金额、材料成本、师傅、客服归属进入施工收入、毛利、师傅工资和客服提成计算。
4. 零售/批发订单进入 `salesOrders`；库存出库写入 `movements`，应同步减少 `products.qty` 并把订单改为已出库。
5. 在途货物 `shipments` 收货后生成 `shipmentReceipts`、必要时生成 `shipmentExceptions`，同时写入入库流水并增加库存。
6. 大仓领料到贴膜间写入 `workshopMovements`；领料减少大仓，施工消耗减少贴膜间结存。
7. 运营费用 `expenses` 和状态为已报销的 `reimbursements` 进入利润表费用。
8. 已交车施工单、商品订单、材料、生产工资、客服提成及费用汇总到报表。

## 已确认的高风险问题

### P0-1 销售单可以绕过库存直接标记已出库

- 服务端允许新增或修改销售单时直接填写“已出库”。
- 只有通过库存出库流水才会减少库存，但手动设置状态不会产生流水。
- 隔离复现：订单数量 2，保存状态“已出库”后，库存 `16000 → 16000`，关联出库流水 `0`。
- 后果：销售统计显示已出库，库存账未减少，销售数量与库存数量必然不匹配。

### P0-2 已出库订单可以删除，留下孤儿库存流水

- 正常的“待出库订单 → 关联出库流水 → 已出库”流程本身可用。
- 但订单出库后仍可删除，服务端没有阻止，也没有做冲销。
- 隔离复现：已出库订单删除成功，关联出库流水仍保留 `1` 条，引用的订单已经不存在。
- 后果：库存减少有记录，但销售收入、应收和客户订单被删除，前后账无法追溯。

### P0-3 在途收货入库流水可被单独撤销

- 在途收货会同时生成入库单、在途到货状态和入库流水。
- 通用“撤销误入库”接口没有禁止撤销带 `shipmentReceiptId` 的流水。
- 隔离复现：撤销后在途单仍为“已到货”，入库单号仍存在，但库存已减回，流水标记已撤销。
- 后果：在途、入库单和库存三套账互相矛盾。

### P0-4 库存商品可在存在业务引用时删除

- 商品已经被订单或出入库流水引用时，服务端仍允许删除商品档案。
- 隔离复现：删除商品后，关联库存流水仍存在，但无法找到对应商品。
- 后果：历史订单成本无法计算、库存流水失去 SKU 主档、毛利可能变成零成本。

## 已确认的计算口径问题

### P1-1 客服提成把排期/待施工订单当成已成交

- `isCommissionableJob()` 目前只排除“取消”和“无效”。
- 只要施工单有价格，排期、预约、待施工、施工中都可能计算成交和提成。
- 同一函数还用于到店率和成交率，导致“到店数 = 成交数”，两个指标失去区别。
- 建议：到店率依据明确的到店状态/到店时间；成交提成至少依据已成交或已交车，并明确退款/返工冲回规则。

### P1-2 商品历史成本没有成交快照

- 销售订单仅保存 SKU、数量和销售单价。
- 毛利计算时读取商品主档的“当前成本”。
- 后续修改商品成本，会让历史订单毛利一起变化；删除商品后历史商品成本变成 0。
- 建议：订单行保存 `unitCostSnapshot`，出库时锁定；历史旧单没有快照时显示“成本待补”，不能静默用当前成本冒充历史成本。

### P1-3 利润表标题写权责发生制，但商品收入按收款确认

- 施工收入：已交车时全额确认，并形成应收账款。
- 商品收入：不管是否出库，只要填写已收金额就确认收入；未出库的普通预收款没有完整进入合同负债。
- 这是两种不同口径，且与页面“权责发生制”标题不一致。
- 需要确定统一政策：若采用权责发生制，商品收入应在出库/交付时确认，出库前收款进入客户预收；若采用收付实现制，则施工收入也应按收款确认，页面不能再称权责发生制。

### P1-4 首页“总收入”与正式利润表不是同一口径

- 首页施工收入使用“只要不是排期/预约/待施工/取消等状态”的订单。
- 首页商品收入使用筛选期全部销售单总价，未排除未出库、未收款等状态。
- 正式利润表则使用已交车施工和已收款商品。
- 后果：同一时间范围的首页收入与财务报表收入天然不相等，但页面没有解释差异。

### P1-5 多师傅施工只有第一位进入工资

- 施工单允许选择多位师傅，但 `primaryInstallerId()` 只取第一位。
- 工资汇总和单笔工费都只归第一位；其他参与师傅仅显示名字，不进入工资。
- 页面文字写“工费按一台车算一次”，但没有定义这笔工费归第一人、平均分、按比例分或团队共享。
- 需要增加每位参与师傅的分配比例/固定金额，且总分配必须等于本单工费。

### P1-6 贴膜间消耗没有可靠关联施工单

- `workshopMovements` 只有自由文本 `jobCustomer`，没有稳定 `jobId`。
- 同名客户、改名、返工或多台车时无法逐单对账。
- 施工单 `materialCost` 也是手填，贴膜间实际消耗不会自动更新施工材料成本。
- 后果：库存实际消耗与施工毛利无法逐单闭环。

## 中风险和缺失链路

- 普通员工工资：系统目前只有师傅计件/底薪加超产；前台、客服、销售、财务等员工的时薪、月薪、加班、请假扣款、报销与工资没有工资总账。
- 打卡/请假/排班：三者目前是记录模块，没有进入工资核算，也没有迟到、缺勤、加班和批准请假的统一工时规则。
- 退款/折让/质保返工：缺少独立贷项、退款及提成/收入/成本冲回凭证；直接改原单会改变历史报表。
- 订单付款：只有累计已付金额，没有每笔收款日期、收款流水、退款流水，无法可靠生成期间现金流和按日期核对银行卡/Zelle/现金。
- 库存账：商品主档“当前库存”可以直接编辑，缺少期初库存、盘盈盘亏、调整单及强制原因，无法严格从流水重算余额。
- 在途异常单：已生成异常记录，但缺少“补货到达、供应商退款、核销、关闭”处理闭环。
- 应付账款：采购成本、运费、关税和供应商付款未形成应付账，库存成本不能包含完整到岸成本。
- 分店维度：客户和 AI 已有分店概念，但施工、库存、员工、销售订单、费用和财务报表没有统一强制 `branchId`，未来两店数据会混算。

## 建议修复顺序

1. 先封住 P0：禁止手工把实物订单直接改成已出库；禁止删除已出库订单和被引用商品；禁止单独撤销在途收货流水。
2. 增加系统“对账检查中心”：每天自动检查订单-出库、收货-入库、SKU 主档、负库存、孤儿引用、重复流水、订单金额和付款状态。
3. 确定收入确认政策并统一首页、报表、应收、客户预收。
4. 订单行锁定历史成本；新增付款流水和退款/冲销单。
5. 把贴膜间消耗强制关联 `jobId`，按实际耗料计算施工单材料成本。
6. 明确多师傅工资分配；把客服提成改为按明确成交节点计算，并支持退款冲回。
7. 建立非师傅员工工资规则，把排班、打卡、批准请假、加班和工资单连接起来。
8. 所有核心业务记录增加 `branchId`，按分店独立库存、收入、费用、工资和报表，再提供公司合并报表。

## 下一轮验证清单

- 下载只读 Railway 正式备份，统计上述问题在正式数据中是否已经存在以及涉及多少条、多少金额。
- 对每个发现输出记录 ID、单号、SKU、数量和差额，但不直接修改正式数据。
- 修复代码后在隔离数据库执行正常、重复、并发、修改、删除、撤销、退款和异常场景回归。
- 部署前备份正式数据库；部署后核对集合数量、库存总量、关键收入、应收、工资和提成不发生非预期变化。

## 2026-08-18 修复与上线结果

- 已封闭四项 P0 入口：禁止绕过库存直接出库、禁止删除已出库订单、禁止单独撤销在途收货流水、禁止删除被引用商品。
- 销售订单行保存历史单位成本快照；正式启动迁移锁定 82 行历史成本。
- 销售订单建立不可覆盖的收款增减明细；正式启动迁移为 72 张历史有收款订单建立期初流水，累计收款没有改变。
- 权责发生制口径统一：施工在交车时确认，商品在库存出库时确认；出库前收款列客户预收，出库后未收列应收。
- 首页与财务报表改用相同收入确认函数。
- 客服提成只在已交车时确认；到店与成交分别计算，不再使用同一数量。
- 多师傅施工的单车工费在所有参与师傅之间平均分配，避免重复计算或只算第一人。
- 贴膜间施工消耗必须关联施工单，并将带成本快照的实际消耗自动汇总到施工单材料成本。
- 商品主档当前库存改为只读；库存只能通过可追溯流水变化。
- GitHub `main`：`adb208d`；Railway deployment：`87fa236e-0a40-4a1a-961f-d5cf373cb695`；健康接口 HTTP 200。
- Railway SSH/SFTP 从当前网络访问超时，因此无法通过 Volume 文件通道下载第二份部署前备份；临时 SSH 密钥已从 Railway 删除。原 Railway 部署保留为代码回滚点，应用自身的日备份机制未被修改。

中风险章节列出的完整普通员工工资总账、退款/贷项单、供应商应付及分店独立总账属于新增业务模块，不是本次以既有数据结构可以安全自动推断的历史账目。本次没有伪造这些历史数据；后续建立时需要先确认工资、退款、应付和分店核算规则。
